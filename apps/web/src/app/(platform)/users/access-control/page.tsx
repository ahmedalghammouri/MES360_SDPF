import { AccessControlView } from '@/features/access-control/access-control-view';

export const metadata = { title: 'Access Control | MES360°' };

export default function AccessControlPage() {
  return <AccessControlView />;
}
